* missing 6502 instructions
  when done, remove "unimplemented instruction" code and messages
* allocate memory and disk buffers dynamically
* allow to hot-load a floppy into the drive
* disk writing
* read-only drives
* support other disk formats
* emulate ESC and Ctrl-C keys (with function keys?)
* graphical mode
* semi-graphical mode
* sound
* game paddles
* command-line options for intercepting NMI, disabling disks, etc
